import one;
import two;

adj = one.getAdjective()
fruits = one.getFruits()


carObject = one.getCarObject()
brands = one.getBrands()
motors = one.getMotors()
colors = one.getColors()

# two.printList(fruits, adj)
# two.printList(brands, colors)
# two.printTwo(colors, motors, brands)
# two.printFancy(colors, motors, brands)

two.printTask(carObject, colors, motors, brands)
